// Exercise 15 — Cloud File Storage.
//
// Build the `FileStorage` class described in README.md. Files live in a single
// global namespace; later levels layer per-user capacity and time-based expiry on
// top of that same namespace, so choose your file representation with Levels 3 and
// 4 already in mind.
//
// The full contract (method names, parameters, return types, the exact
// "name(size)" output format, and the ordering rules) lives in the README.
// Nothing is implemented yet — that's your job.
export class FileStorage {
  // your code here
}
