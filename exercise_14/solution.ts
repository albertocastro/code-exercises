// Exercise 14 — In-Memory Database with TTL + snapshots.
//
// Build the `InMemoryDB` class described in README.md. Start at Level 1 and add
// capability level by level — each level builds directly on the state you already
// have, so design the internal representation with the later levels in mind.
//
// The full contract (method names, parameters, return types, and the EXACT
// "field(value)" output formatting) lives in the README. Nothing is implemented
// yet — that's your job.
export class InMemoryDB {
  // your code here
}
